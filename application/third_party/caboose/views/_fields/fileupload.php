<div class="form-group">
  <label for="{name}" btn btn-default btn-file>{label}
    <input type="file" class="form-control" id="{name}" name="{name}" value="{value}"  
    size="{size}" title="{explain}" {disabled} />    
    <small>Current Image: {value}</small>
  </label>
</div>