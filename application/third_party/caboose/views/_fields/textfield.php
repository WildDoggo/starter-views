<div class="form-group">
  <label for="{name}">{label}</label>
  <input type="text" class="form-control" id="{name}" placeholder="{label}" size="size" title="{explain}"
  	name="{name}" value="{value}" maxLength="{maxlen}" {disabled}>
</div>